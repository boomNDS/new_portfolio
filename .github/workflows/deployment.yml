# File: .github/workflows/deploy.yml
# Name: Deployment
name: Build and Deployment
run-name: Building and Deploying by ${{ github.actor }}

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  # lint-check:
  #   name: Lint
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: write

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0

  #     - name: Install Dependencies
  #       run: yarn install --frozen-lockfile

  #     - name: Lint Check
  #       run: |
  #         if yarn lint; then
  #           echo "Linting passed, no lintfix needed."
  #         else
  #           echo "Linting failed, running lintfix..."
  #           yarn lintfix
  #           # Check if there are any changes to commit
  #           if [[ -n $(git status -s) ]]; then
  #             git config --local user.email "action@github.com"
  #             git config --local user.name "GitHub Action"
  #             git add -A
  #             git commit -m "Fix linting issues"
  #             git push origin main
  #           fi
  #         fi

  build_and_publish:
    name: Build and Publish
    runs-on: ubuntu-latest
    needs: [lint-check]
    permissions:
      contents: read
      deployments: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Build
        run: |
          yarn install --frozen-lockfile
          yarn build
          yarn generate

      - name: Publish to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        id: cloudflare
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: new-portfolio
          directory: dist
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          wranglerVersion: "3"

      - name: Notify Build Status (Success)
        if: success()
        run: |
          BUILD_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="✅ Deployment succeeded for commit ${{ github.sha }}. Access your site at: ${steps.run_tests.outputs.url}. [View build](${BUILD_URL})"

      - name: Notify Build Status (Failure)
        if: failure()
        run: |
          BUILD_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
          -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
          -d text="❌ Deployment failed for commit ${{ github.sha }}. [View build](${BUILD_URL})"